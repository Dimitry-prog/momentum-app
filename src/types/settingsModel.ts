export type IShowSettings = {
  player: boolean;
  weather: boolean;
  time: boolean;
  quote: boolean;
  slider: boolean;
};

export type ISetting = {
  id: number;
  name: string;
  description: string;
};
